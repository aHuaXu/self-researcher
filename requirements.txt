# requirements.txt records the full set of dependencies for development

# PyTorch (install first! required for flash-attn)
# For CUDA 12.4: pip install torch==2.4.0 --index-url https://download.pytorch.org/whl/cu124
# For CUDA 11.8: pip install torch==2.4.0 --index-url https://download.pytorch.org/whl/cu118
# For CPU only: pip install torch==2.4.0
torch==2.4.0

# Core training dependencies (from pyproject.toml)
accelerate
codetiming
datasets
dill
hydra-core
numpy
pandas
peft
pyarrow>=15.0.0
pybind11
pylatexenc
ray>=2.10
tensordict<0.6
torchdata
transformers
wandb

# Optional GPU dependencies (install AFTER torch!)
flash-attn
liger-kernel

# vllm is pinned in pyproject.toml but may need separate install
# vllm<=0.6.3

# Web/API dependencies
flask
requests
beautifulsoup4
html2text
serpapi
google_search_results
pymongo
psutil

# Data processing
openai
openpyxl
xlrd
numexpr

# ML/科学计算
scikit-learn
Bio
pubchempy

# 文件处理
pdfminer
mammoth
python-pptx
puremagic
PyPDF2

# 音频处理
pydub
SpeechRecognition

# 视频处理
youtube_transcript_api

# Document conversion
markdownify

# 其他工具
pathvalidate
python-dotenv
tqdm
chess
sympy
huggingface_hub
modelscope
smolagents
langchain-core
langgraph