# Hi-IGPO Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在现有 DeepResearcher/verl 项目上实现 Hi-IGPO——交替式 Planner-Executor + turn-level 信息增益信用分配,主线为冻结 Executor、只训 Planner 的单 agent 化 IGPO,并支持 A²TGPO 的 turn-group 归一化。

**Architecture:** **以 IGPO 官方代码为基础 port,不从零重写。** 先把 IGPO 的优势函数(`compute_grpo_outcome_advantage` + `_compute_turn_level_advantage`)和 belief 计算整段 port 进本仓库(global 归一化行为用 characterization 测试钉住);**唯一新增的算法**——A²TGPO 的 `turn_group` 归一化 + 变长轨迹 `<2` 回退——单独做真 TDD;再把单 agent IGPO 在现有 rollout 上跑通(2-step smoke);最后新增交替式生成主循环 + 改造多 agent 奖励管理器接入 Phase 2b。设计依据见 `docs/design/hi_igpo_design.md`。

**Tech Stack:** Python 3.10/3.12、PyTorch、verl 0.2.0.dev、vLLM(LoRA hybrid)、Ray、Qwen3-4B-Instruct、pytest。

## Global Constraints

- 算力上限:**4×V100 32GB**;小 batch、`grpo_n` 4~8、`max_planner_turns ≤ 5`。verbatim 来自设计 §2。
- **改远程服务器前先改本地再 rsync**(CLAUDE.md 规则4);服务器:`zjx@10.35.2.238:/home/zjx/self_llm/self-researcher`,conda env `deepresearcher`。
- **优势单测在本地 `.venv-test` 跑**(`uv venv --python 3.12 .venv-test` + CPU torch + pytest;项目自带 `.venv` 是 py3.14 装不了 torch,故另建测试 venv);**CPU 即可,全程 torch、不引入 numpy 重写**;**GPU/生成/训练 smoke 在服务器跑**。
- **训练前检查目标 GPU 空闲**(CLAUDE.md);smoke 用 `trainer.total_training_steps=2`。
- **不整库替换 IGPO,按文件 diff/cherry-pick 移植**;优势/belief 函数体**整段复制后只改命名与 import**(`eos_mask`↔`response_mask` 命名漂移),不重写其数学。
- belief 计算全程 `torch.no_grad()`(= stop-gradient),默认 `gamma=1.0`、`info_gain_norm_mode=separate`。
- **port 的精确入参表示以 Task 0 的 diff 笔记 `docs/design/igpo_port_notes.md` 为准**;后续 Task 1/2 的测试输入按该笔记构造。

---

### Task 0: 准备参考库 + 测试环境 + diff IGPO 优势/belief 签名

本任务产出 port 的"图纸":把 IGPO 优势/belief 函数的**确切签名与输入张量表示**读出来记录成笔记,Task 1/2/3 据此 port。

**Files:**
- Create: `docs/design/igpo_port_notes.md`(IGPO 关键函数签名 + 输入表示 + 与本仓库现状的对照)
- 无代码改动

**Interfaces:**
- Produces: `/tmp/IGPO_ref/`(IGPO 官方仓库);`docs/design/igpo_port_notes.md`(供 Task 1/2/3 port 时查阅);确认 `.venv` 可跑 pytest

- [ ] **Step 1: clone IGPO 参考库**

```bash
git clone --depth 1 https://github.com/GuoqingWang1/IGPO /tmp/IGPO_ref
ls /tmp/IGPO_ref/verl/trainer/ppo/core_algos.py \
   /tmp/IGPO_ref/scrl/llm_agent/vectorized_gt_logprob.py \
   /tmp/IGPO_ref/scrl/llm_agent/prealigned_vectorized.py
```
Expected: 三个文件路径都存在。若 `git clone` 失败(网络/认证),改用 WebFetch 抓 `https://raw.githubusercontent.com/GuoqingWang1/IGPO/main/<path>`(main 不行试 master)。

- [ ] **Step 2: 确认本地测试环境**

```bash
cd /Users/jiahua.xu/dl_learn/self-researcher
.venv/bin/python -c "import torch, numpy, pytest; print(torch.__version__)"
```
Expected: 打印 torch 版本,无 ImportError。

- [ ] **Step 3: 建测试目录**

```bash
mkdir -p tests/hi_igpo && touch tests/hi_igpo/__init__.py
```

- [ ] **Step 4: 读 IGPO 与本仓库的优势/belief 函数,记录签名与表示**

先定位函数行号:
```bash
grep -n "def compute_grpo_outcome_advantage\|def _compute_turn_level_advantage\|AdvantageEstimator" /tmp/IGPO_ref/verl/trainer/ppo/core_algos.py
grep -n "def compute_all_turns_vectorized\|exp(\|mean" /tmp/IGPO_ref/scrl/llm_agent/vectorized_gt_logprob.py
grep -n "info_gain\|log_prob_diff\|turn_end\|vectorized_gt\|belief" /tmp/IGPO_ref/scrl/llm_agent/generation.py
# 本仓库现状对照
grep -n "def compute_grpo_outcome_advantage\|class AdvantageEstimator" verl/trainer/ppo/core_algos.py
```
然后用 **Read 工具**读出 IGPO 的 `_compute_turn_level_advantage` / `compute_grpo_outcome_advantage` 完整函数体,在 `docs/design/igpo_port_notes.md` 里**逐项记录**:
1. `compute_grpo_outcome_advantage` 的完整签名(每个参数名/默认值,尤其 `gamma`、`info_gain_norm_mode` 的取值集合、`curriculum_f1_weight`/`curriculum_ig_weight` 等)。
2. `_compute_turn_level_advantage` 的**输入表示**:它吃的是 token 级 `token_level_rewards (bs,L)` + turn 边界(如 `eos_mask`/turn-end 索引),还是 per-turn 列表?turn 的归一化分组键怎么取(确认它确实是按 prompt 全局 pool,即 `global`)?normalize 用不用 std?normalize→accumulate 的顺序?scatter 回 token 的具体写法?
3. `compute_all_turns_vectorized` 的签名 + belief 公式(确认 `Bel = exp(mean(answer_log_probs))`、全程 `no_grad`)。
4. 本仓库 `compute_grpo_outcome_advantage` 现状(确认它是 vanilla:`sum(-1)` 后按 `index` group-normalize,无 gamma/turn/IG)与 `eos_mask`↔`response_mask` 命名差异。

Expected: 笔记里能回答"Task 1 要复制哪段、改哪几个名字、测试输入张量怎么构造"。

- [ ] **Step 5: Commit**

```bash
git add docs/design/igpo_port_notes.md tests/hi_igpo/__init__.py
git commit -m "docs(igpo): port notes — IGPO advantage/belief signatures & representation"
```

---

### Task 1: 移植 IGPO turn-level 优势进 core_algos(global)+ characterization 钉住

**port 主体。** 把 IGPO 的 `_compute_turn_level_advantage` + `compute_grpo_outcome_advantage` 整段复制进本仓库 `core_algos.py`,重命名为 `compute_igpo_turn_advantage`,**只改命名/import,不改数学**。用 characterization 测试把 global 归一化的输出钉到手算的 IGPO 公式结果,确保 port 无偏差。

**Files:**
- Modify: `verl/trainer/ppo/core_algos.py`(新增 `compute_igpo_turn_advantage`,不动旧 `compute_grpo_outcome_advantage`)
- Test: `tests/hi_igpo/test_igpo_advantage.py`

**Interfaces:**
- Consumes: Task 0 笔记里记录的 IGPO 函数体与入参表示
- Produces(签名以 Task 0 笔记为准,下为目标形态):
  ```text
  compute_igpo_turn_advantage(
      turn_records: dict,   # 本仓库统一的 per-turn 表示,字段(均为 torch.Tensor, 长度 N):
                            #   turn_reward(float)/prompt_id/traj_id/turn_pos(long)
                            #   /is_outcome(bool)/sample_row/span_start/span_end(long)
                            # 函数内部用 torch 转成 IGPO 实际入参形态(token 级 or per-turn,按笔记)
      bs: int, response_len: int,
      gamma=1.0, ig_group_mode="global", info_gain_norm_mode="separate",
      norm_by_std=True, min_group_size=2,
  ) -> (advantages: torch.Tensor(bs,L), returns: torch.Tensor(bs,L))   # returns==advantages
  ```
  > 说明:`turn_records` 是本仓库与 IGPO 之间的**薄 torch 适配层**(Task 6 的 planner 奖励、Task 4 的 generation 钩子都产出它,字段全为 torch.Tensor)。函数内部按 Task 0 笔记把它转成 IGPO `_compute_turn_level_advantage` 真正吃的张量形态后,**复用 IGPO 的 torch 归一化+折扣累加+散射逻辑,不在 numpy 里重写**。

- [ ] **Step 1: 复制 IGPO 优势函数并适配命名**

按 Task 0 笔记,从 `/tmp/IGPO_ref/verl/trainer/ppo/core_algos.py` 复制 `_compute_turn_level_advantage` 与 `compute_grpo_outcome_advantage` 的函数体进本仓库 `core_algos.py`,合并/封装为 `compute_igpo_turn_advantage(turn_records, bs, response_len, gamma, ig_group_mode='global', info_gain_norm_mode='separate', norm_by_std=True, min_group_size=2)`:
- 把 IGPO 的 `eos_mask` 等命名改为本仓库 `core_algos.py` 一致的命名(参考本仓库旧 `compute_grpo_outcome_advantage` 用的是 `response_mask` 还是 `eos_mask`,以 Task 0 笔记为准)。
- `ig_group_mode` 先只接 `'global'`(忠实 port IGPO 的按-prompt-全局归一化);`turn_group` 留给 Task 2。
- 在函数开头加一段薄适配:把 `turn_records` 转成 IGPO 函数体内部使用的张量(token 级 reward + 边界,或 per-turn 数组——按笔记)。
- 顶部 import 复用本仓库已有 `import torch`(必要时 `import numpy as np`)。

- [ ] **Step 2: 写 characterization 测试(钉住 global 归一化数学)**

```python
# tests/hi_igpo/test_igpo_advantage.py
import torch
from verl.trainer.ppo.core_algos import compute_igpo_turn_advantage

# 两条同 prompt 轨迹,各 2 个 IG 中间轮 + 1 个 F1 末轮
#   Traj A: IG=[0.3,0.1], F1=1.0 ; Traj B: IG=[0.1,0.5], F1=0.0
# turn_records 字段全为 torch.Tensor,构造按 Task 0 笔记里 IGPO 的入参表示对齐;
# 期望值按 IGPO 公式(separate 归一化 + mean-only + gamma=1)手算:
#   IG 池 [0.3,0.1,0.1,0.5] mean=0.25 → norm IG=[0.05,-0.15,-0.15,0.25]
#   F1 池 [1.0,0.0] mean=0.5         → norm F1=[0.5,-0.5]
#   归一化后 A=[0.05,-0.15,0.5], B=[-0.15,0.25,-0.5]
#   gamma=1 从后往前累加:A=[0.4,0.35,0.5], B=[-0.4,-0.25,-0.5]
#   散到 token span [0:2,2:4,4:6]
def test_port_global_separate_matches_igpo_formula():
    rec = dict(
        turn_reward=torch.tensor([0.3,0.1,1.0,0.1,0.5,0.0]),
        prompt_id  =torch.tensor([0,0,0,0,0,0]),
        traj_id    =torch.tensor([0,0,0,1,1,1]),
        turn_pos   =torch.tensor([0,1,2,0,1,2]),
        is_outcome =torch.tensor([False,False,True,False,False,True]),
        sample_row =torch.tensor([0,0,0,1,1,1]),
        span_start =torch.tensor([0,2,4,0,2,4]),
        span_end   =torch.tensor([2,4,6,2,4,6]),
    )
    adv, ret = compute_igpo_turn_advantage(
        rec, bs=2, response_len=6,
        ig_group_mode="global", info_gain_norm_mode="separate",
        norm_by_std=False, gamma=1.0)
    assert torch.allclose(adv[0], torch.tensor([0.4,0.4,0.35,0.35,0.5,0.5]), atol=1e-6)
    assert torch.allclose(adv[1], torch.tensor([-0.4,-0.4,-0.25,-0.25,-0.5,-0.5]), atol=1e-6)
    assert torch.allclose(ret, adv)
```
> 若 Task 0 笔记显示 IGPO 默认 `norm_by_std=True`/`info_gain_norm_mode='joint'`,本测试**显式传 `norm_by_std=False, info_gain_norm_mode='separate'`** 锁定上面这组手算数学;std/joint 的行为另由 Task 2 与后续覆盖。

- [ ] **Step 3: Run test to verify it passes**

Run: `.venv-test/bin/python -m pytest tests/hi_igpo/test_igpo_advantage.py -v`
Expected: PASS。若 FAIL,核对 port 时是否漏改命名/边界处理(以 Task 0 笔记为准修正),不要改测试期望值(它们是 IGPO 公式的真值)。

- [ ] **Step 4: Commit**

```bash
git add verl/trainer/ppo/core_algos.py tests/hi_igpo/test_igpo_advantage.py
git commit -m "feat(igpo): port IGPO turn-level advantage into core_algos (global)"
```

---

### Task 2: 扩展 turn_group 归一化 + <2 回退(唯一新数学,TDD)

**IGPO 没有、必须自己写的部分。** 给 Task 1 port 来的 `compute_igpo_turn_advantage` 加 `ig_group_mode='turn_group'`:归一化时不按 prompt 全局 pool,而按 `(prompt_id, turn_pos)` 分组(A²TGPO 思路);某 `(prompt,turn)` 组样本数 `<min_group_size` 时回退到该 reward-type 的全局 prompt 统计(变长 planner 轨迹)。

**Files:**
- Modify: `verl/trainer/ppo/core_algos.py`(在 `compute_igpo_turn_advantage` 的归一化分支里加 `turn_group`)
- Test: `tests/hi_igpo/test_igpo_advantage.py`(追加用例)

**Interfaces:**
- Consumes: Task 1 的 `compute_igpo_turn_advantage`
- Produces: 同函数,`ig_group_mode` 取值扩展为 `"global" | "turn_group"`

- [ ] **Step 1: Write the failing tests(turn_group + 回退)**

```python
# 追加到 tests/hi_igpo/test_igpo_advantage.py
def test_turn_group_separate_matches_handcalc():
    # 同 Task1 的两条轨迹,但按 (prompt,turn_pos) 分组归一化:
    #   IG t0 组 [0.3,0.1] mean=0.2 ; IG t1 组 [0.1,0.5] mean=0.3 ; F1 组 mean=0.5
    #   归一化后 A=[0.1,-0.2,0.5], B=[-0.1,0.2,-0.5]
    #   gamma=1 累加:A=[0.4,0.3,0.5], B=[-0.4,-0.3,-0.5]
    rec = dict(
        turn_reward=torch.tensor([0.3,0.1,1.0,0.1,0.5,0.0]),
        prompt_id  =torch.tensor([0,0,0,0,0,0]),
        traj_id    =torch.tensor([0,0,0,1,1,1]),
        turn_pos   =torch.tensor([0,1,2,0,1,2]),
        is_outcome =torch.tensor([False,False,True,False,False,True]),
        sample_row =torch.tensor([0,0,0,1,1,1]),
        span_start =torch.tensor([0,2,4,0,2,4]),
        span_end   =torch.tensor([2,4,6,2,4,6]),
    )
    adv, _ = compute_igpo_turn_advantage(
        rec, bs=2, response_len=6,
        ig_group_mode="turn_group", info_gain_norm_mode="separate",
        norm_by_std=False, gamma=1.0)
    assert torch.allclose(adv[0], torch.tensor([0.4,0.4,0.3,0.3,0.5,0.5]), atol=1e-6)
    assert torch.allclose(adv[1], torch.tensor([-0.4,-0.4,-0.3,-0.3,-0.5,-0.5]), atol=1e-6)

def test_turn_group_singleton_falls_back_to_global():
    # Traj A 多一个 t2 的 IG 轮(B 没有)→ (prompt,t2) 组只有 1 个 → 回退全局 IG 统计
    # 全 IG(无 F1 末轮),全局 IG mean=(0.3+0.1+0.9+0.1+0.5)/5=0.38
    #   t2 组单样本 → 用全局 → 0.9-0.38=0.52(末轮无累加后继,Ã2=0.52)
    rec = dict(
        turn_reward=torch.tensor([0.3,0.1,0.9,0.1,0.5]),
        prompt_id  =torch.tensor([0,0,0,0,0]),
        traj_id    =torch.tensor([0,0,0,1,1]),
        turn_pos   =torch.tensor([0,1,2,0,1]),
        is_outcome =torch.tensor([False,False,False,False,False]),
        sample_row =torch.tensor([0,0,0,1,1]),
        span_start =torch.tensor([0,2,4,0,2]),
        span_end   =torch.tensor([2,4,6,2,4]),
    )
    adv, _ = compute_igpo_turn_advantage(
        rec, bs=2, response_len=6,
        ig_group_mode="turn_group", info_gain_norm_mode="separate",
        norm_by_std=False, gamma=1.0, min_group_size=2)
    # 行0 t2 token span [4:6] 的优势 = 0.52
    assert torch.allclose(adv[0, 4:6], torch.tensor([0.52, 0.52]), atol=1e-6)
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `.venv-test/bin/python -m pytest tests/hi_igpo/test_igpo_advantage.py -k turn_group -v`
Expected: FAIL(当前 `ig_group_mode='turn_group'` 未实现,报 `ValueError` 或结果不符)。

- [ ] **Step 3: 实现 turn_group 分支**

**核心思路:不写新的归一化数学,只把 IGPO 现成的"按 group 归一化"分组键变细。** IGPO 本来就按 group `index`(prompt 组)做 mean/std 归一化;turn_group 无非把分组键从 `prompt_id` 细化成 `(prompt_id, turn_pos)`。在 `compute_igpo_turn_advantage` 的归一化步骤里(Task 1 port 进来的那段),按 reward-type(separate 时 IG/F1 分开)分别处理:
- `global`:沿用 IGPO 原逻辑(按 `prompt_id` 分组)——回归不破。
- `turn_group`:对 IG 类 turn,分组键 = `prompt_id * MAX_TURNS + turn_pos`;F1 末轮仍按 `prompt_id`。
- 回退:某分组 `size < min_group_size` 的样本,改用其 prompt 级统计(变长 planner 轨迹)。
- 归一化:`(r - mean) / (std + eps)`,`norm_by_std=False` 或组内 `numel<=1` 时 `std=1`。

**全程 torch**,参考骨架(变量名/插入点按 Task 0 笔记对齐 IGPO 实际实现):
```python
def _grouped_mean_std(values, group_id, norm_by_std, eps=1e-6):
    # values:(M,) torch float;group_id:(M,) torch long
    # 返回与 values 同形的 mean/std/size(便于做 <min_group_size 回退)
    mean = torch.zeros_like(values)
    std  = torch.ones_like(values)
    size = torch.ones_like(values)
    for g in torch.unique(group_id):
        m = group_id == g
        v = values[m]
        mean[m] = v.mean()
        size[m] = v.numel()
        if norm_by_std and v.numel() > 1:
            std[m] = v.std(unbiased=False)
    return mean, std, size

def _normalize_turn_rewards(r, prompt_id, turn_pos, ig_group_mode,
                            norm_by_std, min_group_size, max_turns, eps=1e-6):
    # 调用方已按 reward-type 掩码切好 IG / F1 两路,各调一次本函数
    if ig_group_mode == "global":
        mean, std, _ = _grouped_mean_std(r, prompt_id, norm_by_std, eps)
    elif ig_group_mode == "turn_group":
        tg_id = prompt_id * max_turns + turn_pos
        mean, std, size = _grouped_mean_std(r, tg_id, norm_by_std, eps)
        g_mean, g_std, _ = _grouped_mean_std(r, prompt_id, norm_by_std, eps)
        fb = size < min_group_size            # <min 回退到 prompt 级
        mean = torch.where(fb, g_mean, mean)
        std  = torch.where(fb, g_std,  std)
    else:
        raise ValueError(f"unknown ig_group_mode: {ig_group_mode}")
    return (r - mean) / (std + eps)
```
> 这是 Hi-IGPO 第二贡献点(IGPO/上游无现成代码,故在此 TDD);`global` 路径必须与 Task 1 的 characterization 测试一致。`max_turns` 取配置上限(如 `max_planner_turns+1`),确保 `prompt_id*max_turns+turn_pos` 不冲突。

- [ ] **Step 4: Run all advantage tests**

Run: `.venv-test/bin/python -m pytest tests/hi_igpo/test_igpo_advantage.py -v`
Expected: 全部 PASS(global + turn_group + 回退)。

- [ ] **Step 5: Commit**

```bash
git add verl/trainer/ppo/core_algos.py tests/hi_igpo/test_igpo_advantage.py
git commit -m "feat(igpo): turn_group normalization + <min_group_size fallback"
```

---

### Task 3: 移植 belief 计算(GPU 验证)

**Files:**
- Create: `scrl/llm_agent/vectorized_gt_logprob.py`(从 `/tmp/IGPO_ref` 复制 + 改 import)
- Create: `scrl/llm_agent/prealigned_vectorized.py`(同上)
- Test: `tests/hi_igpo/test_belief_smoke.py`(服务器 GPU 跑)

**Interfaces:**
- Produces: `compute_all_turns_vectorized(self, model, input_ids, attention_mask, position_ids, ground_truth_text, turn_end_positions, temperature=1.0) -> (gt_log_probs_per_turn, gt_answer_ranges)`(确切签名以 Task 0 笔记为准);belief `Bel_t = exp(mean(answer_log_probs))`,全程 `torch.no_grad()`。

- [ ] **Step 1: 复制并适配文件**

```bash
cp /tmp/IGPO_ref/scrl/llm_agent/vectorized_gt_logprob.py scrl/llm_agent/
cp /tmp/IGPO_ref/scrl/llm_agent/prealigned_vectorized.py scrl/llm_agent/
.venv/bin/python -c "import ast; ast.parse(open('scrl/llm_agent/vectorized_gt_logprob.py').read())"
```
Expected: 无语法错误。若 import 路径与本仓库不同(如 `from verl...`),按报错逐个改为本仓库实际路径(参考 Task 0 笔记)。

- [ ] **Step 2: 写 belief smoke 测试(GPU)**

```python
# tests/hi_igpo/test_belief_smoke.py
import torch, pytest
@pytest.mark.skipif(not torch.cuda.is_available(), reason="needs GPU")
def test_belief_increases_with_answer_in_context():
    # 用小模型:给两段 context,一段含答案、一段不含,含答案的 belief 应更高
    # 详见服务器实测脚本;断言:0 < Bel < 1 且 Bel_with_answer > Bel_without
    ...
```
> 注:该测试需加载模型,放服务器跑;本步先占位 + 在服务器手验。

- [ ] **Step 3: 服务器手验 belief 数值**

rsync 后在服务器 conda env 写一次性脚本:加载 `Qwen3-4B-Instruct`,固定 question,对比"context 含 golden 答案" vs "无关 context"的 `Bel`,确认 `Bel ∈ (0,1)` 且含答案时更高。
Expected: 数值合理、单调方向正确、无 NaN。

- [ ] **Step 4: Commit**

```bash
git add scrl/llm_agent/vectorized_gt_logprob.py scrl/llm_agent/prealigned_vectorized.py tests/hi_igpo/test_belief_smoke.py
git commit -m "feat(igpo): port vectorized ground-truth belief computation"
```

---

### Task 4: 单 agent IGPO 奖励钩子 + 接入 ray_trainer(GPU smoke)

把"每轮算 IG → 构造 turn_records → 用 Task 1/2 优势"接到现有单 agent rollout。

**Files:**
- Modify: `scrl/llm_agent/generation.py`(cherry-pick IGPO 的 IG 钩子:每轮调用 belief、算 `IG=log_prob_diff`、记录 turn span/pos)
- Modify: `verl/trainer/ppo/ray_trainer.py`(`compute_advantage` 新增 `IGPO` 分支,调用 `compute_igpo_turn_advantage`)
- Modify: `verl/trainer/ppo/core_algos.py`(`AdvantageEstimator` 加 `IGPO='igpo'`)
- Test: 服务器 2-step smoke

**Interfaces:**
- Consumes: `compute_igpo_turn_advantage`(Task 1/2)、belief(Task 3)
- Produces: `data.non_tensor_batch['turn_records']`(generation 阶段填充);`adv_estimator='igpo'` 路径

- [ ] **Step 1: diff IGPO generation.py,定位 IG 钩子**

```bash
grep -n "info_gain\|log_prob_diff\|turn_end\|vectorized_gt\|compute_all_turns" /tmp/IGPO_ref/scrl/llm_agent/generation.py
```
Expected: 列出 IGPO 在 generation 里调用 belief/算 IG 的代码块行号(对照 Task 0 笔记)。

- [ ] **Step 2: cherry-pick IG 钩子到本仓库 generation.py**

把上一步定位的代码块移植进本仓库对应位置:每轮 rollout 结束后调用 `compute_all_turns_vectorized` 得 `Bel_t`,算 `IG_t=Bel_t-Bel_{t-1}`,把 `turn_reward/turn_pos/span_start/span_end/sample_row/traj_id/prompt_id/is_outcome` 累积进 `turn_records`(字段与 Task 1 `compute_igpo_turn_advantage` 入参一致)。适配本仓库变量名(参考 `multi_agent_generation.py` 的 span/turn 记录方式)。

- [ ] **Step 3: 加 AdvantageEstimator.IGPO 并接 compute_advantage**

```python
# core_algos.py 的 AdvantageEstimator 枚举里加:
    IGPO = 'igpo'
```
```text
# ray_trainer.py compute_advantage() 里新增分支:
    elif adv_estimator == AdvantageEstimator.IGPO:
        bs = data.batch['responses'].size(0)
        response_len = data.batch['responses'].size(-1)
        advantages, returns = core_algos.compute_igpo_turn_advantage(
            data.non_tensor_batch['turn_records'], bs=bs, response_len=response_len,
            gamma=gamma,
            ig_group_mode=data.meta_info.get('ig_group_mode', 'global'),
            info_gain_norm_mode=data.meta_info.get('info_gain_norm_mode', 'separate'),
        )
        data.batch['advantages'] = advantages
        data.batch['returns'] = returns
```

- [ ] **Step 4: 本地静态检查**

Run: `.venv/bin/python -c "import ast; [ast.parse(open(f).read()) for f in ['verl/trainer/ppo/core_algos.py','verl/trainer/ppo/ray_trainer.py','scrl/llm_agent/generation.py']]"`
Expected: 无语法错误。

- [ ] **Step 5: rsync + 服务器 2-step smoke(单 agent IGPO)**

在 `scripts/train/grpo_qwen3_4b.sh` 基础上复制 `igpo_single_qwen3_4b.sh`,设 `algorithm.adv_estimator=igpo`、`algorithm.info_gain_type=log_prob_diff`、`algorithm.info_gain_norm_mode=separate`、`algorithm.gamma=1.0`、`trainer.total_training_steps=2`。
```bash
rsync -avz --exclude '.venv' --exclude '.git' ./ zjx@10.35.2.238:/home/zjx/self_llm/self-researcher/
# 服务器:确认 GPU 空闲 → bash scripts/train/igpo_single_qwen3_4b.sh,每 30s 看日志
```
Expected: 完成 step1/2 不 crash;日志里 IG 奖励非零、各 turn advantage 不同、无 NaN。**此步通过 = IG 核心可信。**

- [ ] **Step 6: Commit**

```bash
git add verl/trainer/ppo/core_algos.py verl/trainer/ppo/ray_trainer.py scrl/llm_agent/generation.py scripts/train/igpo_single_qwen3_4b.sh
git commit -m "feat(igpo): single-agent IGPO reward hook + advantage estimator"
```

---

### Task 5: 交替式生成主循环(Planner↔冻结 Executor)

**Files:**
- Create: `scrl/llm_agent/interleaved_generation.py`
- Test: `tests/hi_igpo/test_interleaved_loop.py`(mock executor,本地)

**Interfaces:**
- Consumes: 现有 `LLMGenerationManager.run_llm_loop`(跑冻结 Executor)、`compute_all_turns_vectorized`(belief,Task 3)
- Produces:
  ```text
  class InterleavedGenerationManager:
      def run_loop(self, batch) -> DataProto
      # 外层:Planner 每轮出一个子任务或 <answer>(≤ max_planner_turns)
      # 内层:每个子任务交冻结 Executor 执行,回填 findings
      # 输出含 planner turn 的 turn_records(供 Task 1/2 优势)
  ```

- [ ] **Step 1: Write the failing test(mock executor + mock planner)**

```python
# tests/hi_igpo/test_interleaved_loop.py
from scrl.llm_agent.interleaved_generation import InterleavedGenerationManager

class FakePlanner:
    # 第1轮出子任务,第2轮出 <answer>
    def __init__(self): self.calls = 0
    def step(self, ctx):
        self.calls += 1
        return ("<answer>done</answer>", True) if self.calls >= 2 else (f"subtask-{self.calls}", False)

class FakeExecutor:
    def run(self, subtask, ctx): return f"findings-for-{subtask}"

def test_loop_stops_on_answer_and_caps_turns():
    mgr = InterleavedGenerationManager(planner=FakePlanner(), executor=FakeExecutor(),
                                       max_planner_turns=5)
    trace = mgr.run_loop_pure(question="q")    # 纯 python 版,便于单测
    assert trace["answer"] == "done"
    assert len(trace["subtasks"]) == 1         # 第2轮即 answer
    assert trace["findings"] == ["findings-for-subtask-1"]
```

- [ ] **Step 2: Run test to verify it fails**

Run: `.venv-test/bin/python -m pytest tests/hi_igpo/test_interleaved_loop.py -v`
Expected: FAIL,`ModuleNotFoundError`。

- [ ] **Step 3: Write minimal implementation(先实现可单测的纯循环 `run_loop_pure`)**

```python
# scrl/llm_agent/interleaved_generation.py
class InterleavedGenerationManager:
    def __init__(self, planner, executor, max_planner_turns=5, **kw):
        self.planner = planner
        self.executor = executor
        self.max_planner_turns = max_planner_turns

    def run_loop_pure(self, question):
        """纯 python 控制流(无张量),用于单测与作为 run_loop 的骨架。"""
        subtasks, findings = [], []
        answer = None
        ctx = {"question": question, "findings": findings}
        for _ in range(self.max_planner_turns):
            out, is_answer = self.planner.step(ctx)
            if is_answer:
                answer = out.replace("<answer>", "").replace("</answer>", "")
                break
            subtasks.append(out)
            findings.append(self.executor.run(out, ctx))
        return {"answer": answer, "subtasks": subtasks, "findings": findings}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `.venv-test/bin/python -m pytest tests/hi_igpo/test_interleaved_loop.py -v`
Expected: PASS。

- [ ] **Step 5: 接真实 rollout(在 run_loop_pure 骨架上包真实 Planner/Executor 张量逻辑)**

实现 `run_loop(self, batch)`:复用 `multi_agent_generation.py` 的 DataProto 处理与 `LLMGenerationManager.run_llm_loop`(Executor,**冻结、不回传梯度**);每轮 Planner 生成后调用 belief 算 `Bel_t`,记录 planner turn 的 `turn_reward(IG)/turn_pos/span_start/span_end/traj_id/prompt_id/is_outcome`,末轮写 F1。输出 `DataProto`(含 `turn_records`)。

- [ ] **Step 6: Commit**

```bash
git add scrl/llm_agent/interleaved_generation.py tests/hi_igpo/test_interleaved_loop.py
git commit -m "feat(igpo): interleaved planner-executor generation loop"
```

---

### Task 6: 改造 MultiAgentRewardManager 输出 Planner per-turn IG

**Files:**
- Modify: `verl/workers/reward_manager/multi_agent.py`
- Test: `tests/hi_igpo/test_planner_turn_reward.py`

**Interfaces:**
- Consumes: belief 轨迹 `Bel_0..Bel_T`、golden、F1
- Produces: `build_planner_turn_records(beliefs, f1, traj_id, prompt_id) -> turn_records dict`(字段对接 Task 1 `compute_igpo_turn_advantage`)

- [ ] **Step 1: Write the failing test**

```python
# tests/hi_igpo/test_planner_turn_reward.py
import torch
from verl.workers.reward_manager.multi_agent import build_planner_turn_records

def test_ig_from_beliefs_and_f1_terminal():
    beliefs = [0.1, 0.4, 0.45, 0.9]   # Bel_0..Bel_3 → 3 个子任务
    rec = build_planner_turn_records(beliefs=beliefs, f1=1.0, traj_id=0, prompt_id=0)
    # IG = [0.3, 0.05, 0.45] (中间轮) ; 末轮 F1=1.0
    assert torch.allclose(rec["turn_reward"], torch.tensor([0.3, 0.05, 0.45, 1.0]), atol=1e-6)
    assert rec["is_outcome"].tolist() == [False, False, False, True]
    assert rec["turn_pos"].tolist() == [0, 1, 2, 3]
```

- [ ] **Step 2: Run test to verify it fails**

Run: `.venv-test/bin/python -m pytest tests/hi_igpo/test_planner_turn_reward.py -v`
Expected: FAIL,`ImportError`。

- [ ] **Step 3: Write minimal implementation(append to multi_agent.py)**

```python
import torch

def build_planner_turn_records(beliefs, f1, traj_id, prompt_id):
    beliefs = torch.as_tensor(list(beliefs), dtype=torch.float32)
    igs = beliefs[1:] - beliefs[:-1]                                   # 中间轮 IG
    turn_reward = torch.cat([igs, torch.tensor([float(f1)])])
    n = turn_reward.numel()
    return dict(
        turn_reward=turn_reward,
        is_outcome=torch.tensor([False] * (n - 1) + [True]),
        turn_pos=torch.arange(n),
        traj_id=torch.full((n,), traj_id),
        prompt_id=torch.full((n,), prompt_id),
    )
```
> `sample_row/span_start/span_end` 由 Task 5 的交替式生成在拼 batch 时补齐(此处只产出 reward/分组字段),全为 torch.Tensor。

- [ ] **Step 4: Run test to verify it passes**

Run: `.venv-test/bin/python -m pytest tests/hi_igpo/test_planner_turn_reward.py -v`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add verl/workers/reward_manager/multi_agent.py tests/hi_igpo/test_planner_turn_reward.py
git commit -m "feat(igpo): planner per-turn IG reward records"
```

---

### Task 7: 接线 Phase 2b + config + 服务器 smoke

**Files:**
- Modify: `verl/trainer/ppo/ray_trainer.py`(multi-agent 分支:enable interleaved 时用 `InterleavedGenerationManager` + `igpo` 优势 + 冻结 Executor LoRA)
- Create: `scripts/train/hi_igpo_phase2b_qwen3_4b.sh`
- Modify: `docs/design/dual_agent_smoke_test.md`(追加 Hi-IGPO smoke 记录段)

**Interfaces:**
- Consumes: Task 4(优势 + estimator)、Task 5(交替式生成)、Task 6(planner IG 奖励)

- [ ] **Step 1: 接线 multi-agent 分支**

在 `ray_trainer.py` 现有 `multi_agent.enable` 分支内:当 `multi_agent.interleaved.enable=true` 时,用 `InterleavedGenerationManager` 跑 rollout、`build_planner_turn_records` 造奖励、`adv_estimator='igpo'` 算优势,且 **Executor LoRA `requires_grad=False`/不进 optimizer**,只更新 Planner LoRA。

- [ ] **Step 2: 写训练脚本**

复制 `scripts/train/grpo_dual_agent.sh` 为 `hi_igpo_phase2b_qwen3_4b.sh`,新增:
```bash
multi_agent.interleaved.enable=true \
multi_agent.max_planner_turns=5 \
algorithm.adv_estimator=igpo \
algorithm.info_gain_type=log_prob_diff \
algorithm.info_gain_norm_mode=separate \
algorithm.ig_group_mode=turn_group \
algorithm.gamma=1.0 \
multi_agent.freeze_executor=true \
trainer.total_training_steps=2 \
data.train_batch_size=4 agent_grpo.n=2 actor_rollout_ref.rollout.gpu_memory_utilization=0.4
```

- [ ] **Step 3: 本地静态检查 + 跑全部纯单测**

Run: `.venv-test/bin/python -m pytest tests/hi_igpo -v`
Expected: 所有纯函数/优势测试 PASS。

- [ ] **Step 4: rsync + 服务器 2-step smoke(Phase 2b)**

```bash
rsync -avz --exclude '.venv' --exclude '.git' ./ zjx@10.35.2.238:/home/zjx/self_llm/self-researcher/
# 服务器:确认 GPU 0-3 空闲 + SearXNG 已启动 → bash scripts/train/hi_igpo_phase2b_qwen3_4b.sh
```
每 30s 监控日志。Expected: 完成 step1/2,Planner rollout 出子任务/`<answer>`,IG/优势数值合理无 NaN,仅 Planner LoRA 更新。

- [ ] **Step 5: 记录 smoke 结果 + Commit**

把 smoke 配置变更与遇到的问题追加进 `docs/design/dual_agent_smoke_test.md`。
```bash
git add scripts/train/hi_igpo_phase2b_qwen3_4b.sh verl/trainer/ppo/ray_trainer.py docs/design/dual_agent_smoke_test.md
git commit -m "feat(igpo): wire Hi-IGPO phase 2b (interleaved + frozen executor)"
```

---

### Task 8: 消融脚本(global vs turn_group / separate vs joint)

**Files:**
- Create: `scripts/train/hi_igpo_ablation.sh`(参数化跑各消融档)

**Interfaces:**
- Consumes: Task 7 的训练入口与 config 开关

- [ ] **Step 1: 写消融脚本**

以 `hi_igpo_phase2b_qwen3_4b.sh` 为模板,用环境变量切换 `algorithm.ig_group_mode={global,turn_group}`、`algorithm.info_gain_norm_mode={separate,joint}`,各跑一档(去掉 `total_training_steps=2` 跑完整;算力紧时减小数据规模)。对照设计 §8 的消融表。

- [ ] **Step 2: 本地静态检查**

Run: `bash -n scripts/train/hi_igpo_ablation.sh`
Expected: 无语法错误。

- [ ] **Step 3: Commit**

```bash
git add scripts/train/hi_igpo_ablation.sh
git commit -m "feat(igpo): ablation runner for normalization modes"
```

---

## Self-Review

**Spec coverage(对照 design §2-§10):**
- §3 交替式架构 → Task 5
- §4 belief/IG → Task 3(belief)+ Task 4(IG 钩子)
- §5.0 单 agent 化 → Task 4;§5.1 归一化+累加(IGPO port)→ Task 1;§5.2 global → Task 1;turn_group + 回退 → Task 2;separate/joint → Task 1/2;§5 散射到 token → Task 1(随 port 一并落地)
- §6 Phase 1 单 agent IGPO → Task 4;Phase 2b → Task 7
- §7 移植步骤 → Task 0(diff 笔记)/Task 1(优势 port)/Task 3(belief port)/Task 4(generation cherry-pick);§8 消融 → Task 8;§9 风险(先单 agent 验数值)→ Task 4 Step5 gate
- 未覆盖:§7 `prealigned_vectorized.py` 仅复制(Task 3),其在 generation 的具体用法随 Task 4 cherry-pick 落地;自适应 turn 级 clipping(§5.2 可选)未排任务 → 标注为后续可选,不阻塞主线。

**Placeholder scan:** Task 3 Step2 的 belief 单测为占位(需模型),已显式说明放服务器手验(Task 3 Step3),非纯函数路径不强求 TDD——属合理工程取舍。Task 1/2 的"输入张量构造按 Task 0 笔记"不是占位:它是 port 的固有依赖(需先读 IGPO 真实入参表示),且**期望数值已用 IGPO 公式给死**。其余步骤均含可运行代码/命令。

**Type consistency:** `turn_records` 字段(turn_reward/prompt_id/traj_id/turn_pos/is_outcome/sample_row/span_start/span_end)在 Task 1/2/4/6 一致;`compute_igpo_turn_advantage` 签名 Task 1 定义、Task 2 扩展 `ig_group_mode`、Task 4 调用一致;`build_planner_turn_records` Task 6 定义产出 reward/分组字段,span/row 由 Task 5 补齐;`InterleavedGenerationManager` Task 5 定义。

**Port-first 一致性:** Task 1 明确"整段复制 IGPO 函数体、只改命名、不改数学",characterization 测试用 IGPO 公式真值钉住;唯一新数学(turn_group)隔离在 Task 2 做 TDD——符合"以 IGPO 代码为基础"的约束。

**torch-native 一致性:** 全链路无 numpy 重写——`turn_records` 字段(Task 1/2/4/6)、`build_planner_turn_records`(Task 6)、turn_group 归一化(Task 2 的 `_grouped_mean_std`/`_normalize_turn_rewards`)、所有优势单测断言均为 torch.Tensor + `torch.allclose`,CPU 即可跑;复用 IGPO 的 torch 归一化机制,只把分组键变细。
